//! Domain vocabulary and rules that need no other layer.
//!
//! See DESIGN.md §3. `predicate_keys` owns which predicate keys exist;
//! `predicates` owns what each one means.

pub mod predicate_keys;
pub mod predicates;
