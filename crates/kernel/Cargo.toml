[package]
name = "companion-memory-kernel"
description = "Pure decision kernel for companion memory: predicate registry, record identity, evidence graph, mention gate, scoring."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
