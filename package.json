{
  "name": "companion-memory-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "面向 DeepSeek Harness 的伴侣记忆：纯决策内核与 DSH 插件适配器。",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.16"
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "build:worker:windows": "cargo build --release --target x86_64-pc-windows-msvc -p companion-memory-worker && node scripts/package-worker.mjs --target x86_64-pc-windows-msvc",
    "build:worker:linux": "cargo build --release --target x86_64-unknown-linux-gnu -p companion-memory-worker && node scripts/package-worker.mjs --target x86_64-unknown-linux-gnu"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
