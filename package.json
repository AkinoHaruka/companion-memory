{
  "name": "companion-memory-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Companion memory for DeepSeek Harness: a pure decision kernel plus a DSH plugin adapter.",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.16"
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
